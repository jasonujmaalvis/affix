/**
*
* @filename     affix.js
* @author       Jason Alvis
* @plugin       Affix
*
*/
(function($){
    "use strict";

    $.fn.affix = function(options) {
        // passing destruct: true allows
        var settings = $j.extend({
            destruct: false
        }, options);

        return this.each(function() {

            // accessing settings
            // settings.destruct

        });
    };

})(jQuery);